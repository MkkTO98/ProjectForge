# Problem Index

Use `ArchitectureHarvest/retrieval/problem_catalog.yaml` as the canonical problem-first index.

This Markdown index is a human navigation layer. Keep entries concise and point to problem IDs, synthesized patterns, contradictions, and outcome records rather than copying reports.

## Seed problems

- `long_running_agent_state` -> checkpointing, event history, resumable execution
- `architecture_decision_reuse` -> problem-first retrieval, synthesized pattern cards, outcome-weighted recommendations
