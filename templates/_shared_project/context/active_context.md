# Active Context

Artifact role: generated context bundle for a specific task/model target.

Generated by `tools/build_context.py` or by a context manager for a specific task/model target.

This is an output artifact, not mandatory startup context. Do not load stale `context/active_context.md` by default. Rebuild a fresh task-specific bundle when explicit context bundling is needed.

This file should contain selected Priority 1 and task-specific context, not the entire repository.
