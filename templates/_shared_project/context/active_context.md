# Active Context

Generated by `tools/build_context.py` or by the context manager agent for a specific task/model target.

This is an output artifact, not mandatory startup context. Do not load stale `context/active_context.md` by default. Rebuild a fresh task-specific bundle when explicit context bundling or cloud governance is needed.

This file should contain the current task-specific context bundle, not the entire repository.
