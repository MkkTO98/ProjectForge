# Skill: Logging Workflow

Logging is always enabled.

For each meaningful run, record:
- agent
- goal
- status
- files changed
- validation results
- decisions created
- questions created

Use `tools/log_run.py` where possible.
