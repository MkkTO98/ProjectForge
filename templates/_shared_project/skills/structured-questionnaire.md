# Structured Questionnaire


Use `config/setup_questionnaire.yaml` as the baseline interview source. The model may ask contextual follow-ups, but it must not replace or silently ignore the structured questionnaire.

Use `config/sufficiency_policy.yaml` to decide whether the project can be initialized or whether a blocking clarification is required.

