# ChatGPT Export Recovery Import Area

This directory is reserved for curated recovery notes, not raw exports.

Raw ChatGPT exports and old scaffold archives must stay outside normal context unless the task is explicitly recovery, forensic, or failure investigation. Future agents should use `context/reconstruction/` first.
