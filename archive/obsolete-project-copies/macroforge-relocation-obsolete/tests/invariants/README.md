# Invariant Tests

Invariant tests enforce ProjectForge's own operating rules. They are not ordinary application tests.

Examples:
- Required policy files exist.
- SQLite indexing is not enabled by default.
- GitHub push remains manual by default.
- Knowledge graph remains simple.
- Specialized agents require request-before-generation.
